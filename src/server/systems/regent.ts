/**
 * The regent lives in `./regent/` now — one file per rule, a situation they
 * all read, and a temperament that makes each nation's steward its own
 * (decision 0028). This module keeps the import path every other file uses.
 */

export { regentSystem } from "./regent/index";
